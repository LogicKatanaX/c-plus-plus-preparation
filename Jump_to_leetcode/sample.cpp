#include<bits/stdc++.h>
using namespace std;
int main(){
    vector <int> arr;
    arr.push_back(10);
    arr.push_back(20);
    arr.push_back(30);
    int length=arr.size();
    for(int i=0;i<length;i++){
        cout<<arr[i]<<" ";
    }
    return 0;
}